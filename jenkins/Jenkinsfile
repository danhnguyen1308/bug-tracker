pipeline {
    agent any

    stages {
        stage('UnitTest Tests - Backend') {
            agent{
                docker {
                    image 'golang:latest'
                    reuseNode true
                }
            }
            steps {
                dir('bugtracker-backend') {
                    sh 'go test -v ./...'
                }
            }
        }
    }
}
