pipeline {
    agent any

    stages {
        stage('UnitTest Backend -Danh') {
            steps {
                dir('bugtracker-backend') {
                    go test -v ./...
                }
            }
        }
    }
}
